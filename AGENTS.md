# AGENTS.md

本文件为 Codex 在此工作区工作时的指南。它说明项目背景、比赛规则要点、开发环境与推进路线，以及最重要的——与用户的协作方式。

## 1. 项目概述与任务目标

### 这是什么

- 赛事：全国大学生机器人大赛 ROBOTAC · AIROBOTIC 创新挑战赛，**总决赛 C 组（无人机赛道）**
- 本仓库：`YunDrone-Team/robotac`（官方/upstream）→ fork 至 `6hao666/robotac`（队伍/origin），本地工作区即此仓库
- 指定无人机平台：云纵科技 Sunray-150-ROBOTAC（下视相机 + Mid-360 激光雷达），飞控 NxtPX4 v2

### 任务目标

队伍最终需同时完成四类成果：

1. 能在指定飞机上稳定运行的实际比赛程序
2. 能完成 C1-C5 自主任务闭环（起飞 → 绕障 → 识别对准 → 投放 → 返航 → 降落）
3. 能证明部署、定位、视觉、投放、安全接管与实机测试的预审材料
4. 能解释技术方案、问题分析与迭代过程的答辩材料

## 2. 比赛规则要点（以《2026-8-12_C组无人机竞赛规则.pdf》为准）

### 总分构成（满分 100）

| 模块 | 分值 | 阶段 |
| --- | --- | --- |
| 模块一：真机系统搭建与调试 | 22 | 赛前线上预审 |
| 模块三：工程资料完整性 | 18 | 赛前线上预审 |
| 模块二：无人机真机任务执行 | 35 | 决赛现场 |
| 专家答辩 | 15 | 决赛现场 |
| 创新加分 | 10 | 决赛现场 |

### 场地与器材

- 有效飞行区 `4.00m × 5.00m`；场地净高暂定 `3.00m`
- 起降台 / 投放台：两张 `Ø0.80m` 圆桌，桌面高 `0.75m`，圆心 `(2.00, 0.80)` 与 `(2.00, 4.20)`
- 固定障碍物：`2.10m × 0.15m × 2.00m`，场地中部横向，两侧净宽各 `0.95m`；**禁止从障碍物顶部越过**
- AprilTag：`Tag36h11` / `ID 0` / 黑色编码区边长 `150mm`；起降台与投放台使用**相同**的 Tag
- 模拟货物：圆盘式挂载重物 `100–110g`，外径 `Ø52mm`，耳座通孔 `Ø4mm`

### 任务流程 C1 → C5（单次满分 35）

- **C1 自主起飞与状态建立（5 分）**：自主解锁起飞至 `1.00–2.00m` 高度，稳定保持 ≥ 3s
- **C2 自主飞行与固定障碍绕行（8 分）**：接近障碍 → 任一侧安全绕行 → 进入目标区域
- **C3 固定目标视觉识别与自主对准（8 分）**：机载视觉识别 → 中心投影进入投放台边界 → 悬停 ≥ 3s
- **C4 模拟货物自主释放与落点精度（9 分）**：程序自主触发投放（3 分）+ 落点区域（6 分：精准区 Ø0.40m 内 / 良好区 0.40–0.60m / 有效区 0.60–0.80m）
- **C5 自主返航、再次绕障与降落（5 分）**：返程绕障（2 分）+ 降落区域（3 分：良好 Ø0.60m 内 / 有效 0.60–0.80m）

### 关键规则

- 每队最多两次正式飞行，共用连续 **6 分钟**（含复位与重新挂载），取单次完整成绩的较高值，**不可跨飞行拼分**
- 两次飞行须使用同一架登记无人机、同一套程序；不得使用预录画面或人工控制代替自主运行
- 落点与降落均以几何中心投影判定；安全边界以标线内侧为判定线，越界即终止
- 预审需提交：真机运行与测试说明 PDF、工程文件压缩包、真机预演/功能测试视频（不要求完整赛道一次成功）
- 线上预审材料无法证明的内容不得按满分认定；C 组不强制提交仿真材料

## 3. 开发环境与推进路线

### 环境

- 机载电脑：Ubuntu 20.04.6 LTS / ARM64 / ROS Noetic / Python 3.8
- 连接：E8372 热点 `ROBOTAC-TEAM` → SSH `yundrone@<动态IP>`（在 `192.168.0.1` 管理页查主机名 yundrone 或 MAC `fc:37:6d:bd:1f:d2`）
- 关键设备：NxtPX4 v2、MID360 激光雷达、FAST-LIO、RGB 相机、AprilTag、MAVROS、外部视觉桥、舵机投放机构

### 推进阶段（必须按顺序）

`A 统一代码与离线测试 → B 真实硬件只读接入 → C 定位/视觉/坐标系验证 → D 拆桨地面联调 → E 单项受控实飞 → F 完整比赛程序（C1-C5 状态机）→ G 完整赛道重复性测试 → H 预审/答辩材料`

### 常用命令

```bash
cd ~/robotac_ws && source /opt/ros/noetic/setup.bash && source devel/setup.bash
./tools/test_01_source.sh            # 源码检查
./tools/test_02_build.sh             # 构建
./tools/test_03_unit.sh              # 单元测试（尚无通过记录）
./tools/test_04_simulation.sh        # 仿真
./tools/test_05_hardware_readonly.sh # 真实硬件只读检查
roslaunch robotac_bringup sensors.launch        # 传感器
roslaunch robotac_bringup perception.launch     # FAST-LIO + AprilTag
roslaunch robotac_bringup flight_base.launch    # 飞控基础链路
roslaunch robotac_examples 01_fcu_state.launch  # 01-11 系列示例
```

### 关键注意

- 两个圆桌使用**相同 Tag ID 0**，程序不能只凭 Tag ID 判断位置，需结合任务阶段 + FAST-LIO 本地位置 + 预期区域
- 舵机释放成功 ≠ 货物完全脱离，需视频等可核验方式确认
- 禁止使用 `git reset --hard` 清理不明修改；统一版本时记录提交 SHA

## 4. 与用户的协作方式（重要）

- **不要一味附和**：不要认为用户说的都是对的。对用户的要求和目标应批判性提出建议，而不是盲目执行。
- **不明确就问**：当用户的需求不清晰时，**一次只问一个问题**，直到完全明确用户的具体需求为止。
- **确认后执行**：理解需求后先**复述**给用户确认，得到认可后才动手操作。
- 发现用户说法与事实或代码矛盾时，应指出并说明依据，而不是默默照做。
